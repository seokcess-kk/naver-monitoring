{
  "name": "workspace",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "deploy:build": "npm run deploy:build --prefix Search_Scope",
    "deploy:start": "npm run deploy:start --prefix Search_Scope",
    "build": "cd Search_Scope && npm run db:push && npm run build",
    "start": "cd Search_Scope && npm run start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bullmq": "^5.66.5",
    "ioredis": "^5.9.2",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  }
}
